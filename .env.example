# =============================================================================
# AMLIC Project Environment Variables
# Copy this file to .env and fill in your values:  cp .env.example .env
# =============================================================================

# HuggingFace token — needs gated access to meta-llama/Llama-3.2-3B-Instruct
HF_TOKEN=hf_your_token_here

# Model to serve
MODEL_NAME=meta-llama/Llama-3.2-3B-Instruct

# ---------------------------------------------------------------------------
# VM IPs (fill in after setting up Tailscale)
# ---------------------------------------------------------------------------
VM_PREFILL_IP=<PREFILL_TAILSCALE_IP>
VM_DECODE_IP=<DECODE_TAILSCALE_IP>
VM_COLOCATED_IP=${VM_PREFILL_IP}

# ---------------------------------------------------------------------------
# Ports
# ---------------------------------------------------------------------------
COLLOCATED_PORT=8000
PREFILL_PORT=8100
DECODE_PORT=8200
PROXY_PORT=9000

# ---------------------------------------------------------------------------
# Redis (LMCache shared KV cache storage — runs on prefill VM)
# ---------------------------------------------------------------------------
REDIS_HOST=${VM_PREFILL_IP}
REDIS_PORT=6379

# ---------------------------------------------------------------------------
# vLLM settings
# ---------------------------------------------------------------------------
MAX_MODEL_LEN=4096
GPU_MEM_UTIL=0.85

# ---------------------------------------------------------------------------
# Router config
# ---------------------------------------------------------------------------
ROUTING_THRESHOLD=500

# Endpoint URLs (derived)
COLLOCATED_URL=http://${VM_COLOCATED_IP}:${COLLOCATED_PORT}/v1
DISAGG_PROXY_URL=http://${VM_PREFILL_IP}:${PROXY_PORT}/v1

# ---------------------------------------------------------------------------
# GCP cost rates ($/hr, on-demand, us-central1)
# ---------------------------------------------------------------------------
GCP_L4_COST_PER_HOUR=0.71
GCP_T4_COST_PER_HOUR=0.35
